import React from 'react';
import { View } from 'react-native';

// import { Container } from './styles';

interface PageProps {
  salvar: () => void;
}

const Appointment: React.FC<PageProps> = ({ salvar }) => <View />;

export default Appointment;
